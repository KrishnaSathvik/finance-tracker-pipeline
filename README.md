echo "# Finance Tracker Pipeline

A data engineering project to process financial transactions from a CSV, store them in SQLite, and analyze them with SQL queries.

## Setup
1. Install dependencies: \`pip install pandas\`
2. Run the pipeline: \`python pipeline.py\`
3. Run the queries: \`python queries.py\`

## Files
- \`pipeline.py\`: ETL pipeline script.
- \`queries.py\`: Practice SQL queries.
- \`transactions.csv\`: Sample financial data.
" > README.md